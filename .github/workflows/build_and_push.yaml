name: Build and push Ansible execution environment

on:
  push:
    branches:
      - master


concurrency:
  cancel-in-progress: true
  group: ${{ github.workflow }}-${{ github.ref }}


permissions:
      contents: read
      packages: write
      id-token: write


env:
  REGISTRY: ghcr.io
  ORGANIZATION: jirifilip
  IMAGE_NAME: ${{ repository.name }}


jobs:
  build-and-push:
    name: Build and push
    runs-on: ubuntu-latest
    env:
      IMAGE_TAG: "${{ env.REGISTRY }}/${{ env.ORGANIZATION }}/${{ IMAGE_NAME }}:latest"
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup uv
        uses: astral-sh/setup-uv@v7

      - name: Install uv project
        run: |
          uv sync

      - name: Build execution environment image
        run: >
          uv run
          ansible-builder build 
          --container-runtime=docker
          --tag ${{ env.IMAGE_TAG }}

      - name: Log into registry
        uses: docker/login-action@v3.6.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Push image
        run: |
          docker push ${{ env.IMAGE_TAG }}
